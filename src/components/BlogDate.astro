---
interface Props {
    date: Date;
}
const { date } = Astro.props;
---

<p class="text-skin-base font-opensans">
    {date.toLocaleDateString("en-US", { year: "numeric", month: "long", day: "numeric" })}
</p>
