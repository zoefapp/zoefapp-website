---
import type { HTMLAttributes } from "astro/types";
interface Props extends HTMLAttributes<"label"> {
    className?: string;
}
import { cn } from "~/lib/utils";
const { className, ...props } = Astro.props;
---

<label class={cn("text-skin-base/80", className)} {...props}>
    <slot />
</label>
