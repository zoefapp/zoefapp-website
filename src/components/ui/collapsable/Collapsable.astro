---
interface Props {
    className?: string;
}
const { className } = Astro.props;
import { cn } from "~/lib/utils";
---

<div x-data="{ open: false }" class={cn("flex flex-col gap-8 border rounded-[24px] p-4 md:p-8 mt-8", className)}>
    <slot />
</div>
