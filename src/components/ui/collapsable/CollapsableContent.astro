---
interface Props {
    className?: string;
}
const { className } = Astro.props;
import { cn } from "~/lib/utils";
---

<div x-show="open" class={cn("animate-in fade-in duration-200", className)}>
    <slot />
</div>
