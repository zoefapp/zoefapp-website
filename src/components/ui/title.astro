---
import { cn } from "~/lib/utils";
---

<h1
    class={cn(
        "text-skin-base text-[clamp(2.4rem,4vw,3.5rem)] leading-[1.11] font-bold max-lg:text-balance lg:whitespace-pre-line"
    )}
>
    <slot />
</h1>
