---
import ContactLayout from "~/layouts/ContactPage.astro";
export const prerender = true;

import { getPageContent } from "~/lib/page";
import { getLanguage } from "~/i18n/utils";

let pageContent = await getPageContent(getLanguage(Astro), "contactus");
---

<ContactLayout page={pageContent} />
